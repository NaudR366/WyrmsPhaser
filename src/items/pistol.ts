import Phaser from 'phaser'
import Gun from './gun';

export default class Pistol extends Gun {
    // euhhuefdjf??

    constructor(scene, x, y, key = 'yeet', frame = 'aaa'){
        super(scene, x, y, key, frame);   

        this.body.setSize(this.)
    }

    update(time: number, delta: number): void{
        this.x;
    }



}